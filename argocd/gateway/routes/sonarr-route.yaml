apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr-route
  namespace: sonarr
  annotations:
    sonarr.argoproj.io/sync-wave: "2"
spec:
  hostnames:
    - sonarr.hometheo.click
  parentRefs:
    - name: main-gateway
      namespace: default
      sectionName: https
  rules:
    - backendRefs:
        - name: sonarr
          port: 8989
