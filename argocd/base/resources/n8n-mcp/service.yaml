apiVersion: v1
kind: Service
metadata:
  name: n8n-mcp
  namespace: n8n-mcp
  labels:
    app: n8n-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: n8n-mcp
