# n8n MCP HTTPRoute - etcd.me overlay
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: n8n-mcp-route
  namespace: n8n-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  parentRefs:
    - name: main-gateway
      namespace: default
      sectionName: https-mcp
  hostnames:
    - n8n.mcp.etcd.me
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: n8n-mcp
          port: 8080
